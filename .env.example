# -----------------------------------------------------------------------------
# Frontend (Vite) Environment Variables
# -----------------------------------------------------------------------------
# Supabase client configuration used by the public web app
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# WOMPI payment configuration consumed by the frontend when creating links
VITE_WOMPI_APP_ID=your-wompi-app-id
VITE_WOMPI_API_SECRET=your-wompi-api-secret
# Optional: override to use sandbox/production urls
VITE_WOMPI_API_URL=https://api.wompi.sv/v1

# -----------------------------------------------------------------------------
# Supabase Edge Function Environment Variables
# (set via `supabase secrets set` or the dashboard; duplicate locally for serve)
# -----------------------------------------------------------------------------
# Supabase service credentials so functions can read/write with elevated access
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# WOMPI server-side credentials reused by the Edge Functions
WOMPI_APP_ID=your-wompi-app-id
WOMPI_API_SECRET=your-wompi-api-secret
WOMPI_API_URL=https://api.wompi.sv

# Email notification settings (Resend)
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM="Cáceres Videogames" <orders@caceresvideogames.com>
ADMIN_NOTIFICATION_EMAIL=rcaceres.a@outlook.com

# Optional: include additional recipients separated by commas if needed
# ADMIN_NOTIFICATION_CC=
