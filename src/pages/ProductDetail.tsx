// Deprecated. Product details have been merged into Products page via the `productId` query param.
// This file is intentionally left as a stub to avoid broken imports while migrating.
// Do not use. Remove after verifying routes and links.

export default function ProductDetailDeprecated() {
  return null;
}
